{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-control",
  "description": "Minimal local UI permissions for the KINGAIBOT Windows and macOS control center.",
  "local": true,
  "windows": ["main"],
  "platforms": ["windows", "macOS"],
  "permissions": [
    "core:default",
    "stronghold:default",
    "notification:default",
    "os:default"
  ]
}
